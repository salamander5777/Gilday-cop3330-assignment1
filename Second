/*
 *  UCF COP3330 Fall 2021 Assignment 1 Solution
 *  Copyright 2021 Michael Gilday
 */

import java.util.Scanner;

//Exercise 2 - Counting the Number of Characters. (Prompts for an input string and displays output that shows the input string and the number of characters the string contains.)
class Main {
  public static void main(String[] args) {
    Scanner string_find = new Scanner(System.in);  //Creation of a scanner object to allow for user input.

    System.out.println("What is the input string? ");
    String input_string = string_find.nextLine();  //Used to read in the name.
    int b = string.length(input_string);
    System.out.println(input_string +" has " + b + " characters in it.");  //Prints out statement and the input name.
    string_find.close();
  }
}